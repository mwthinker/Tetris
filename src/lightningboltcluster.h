#ifndef LIGHTNINGBOLTCLUSTER_H
#define LIGHTNINGBOLTCLUSTER_H

#include "lightningbolt.h"
#include "vertexdata.h"
#include "tetrisentry.h"
#include "dynamicbuffer.h"

#include <vector>

class LightningBoltCluster {
public:
	LightningBoltCluster(DynamicBuffer& buffer, TetrisEntry spriteEntry, Vec2 source, Vec2 dest) {

	}

	void draw(float deltaTime, const BoardShader& boardShader) {

	}

private:

};

#endif // LIGHTNINGBOLTCLUSTER_H
